#include <stdio.h>

/**
 * main - prints the phrase "programming is like building a multiple puzzle"
 * retun: 0 if the properly, non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like buildong a multilingual puzzle");
	return (0);
}

